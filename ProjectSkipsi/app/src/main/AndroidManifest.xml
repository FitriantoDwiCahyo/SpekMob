<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectskipsi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MobilVerJenis"></activity>
        <activity android:name=".ListReview" />
        <activity android:name=".Quiz" />
        <activity android:name=".SpesifikasiMobil" />
        <activity android:name=".MobilVerBrand" />
        <activity android:name=".Review" />
        <activity android:name=".Editprofile" />
        <activity android:name=".ProfileUsers" />
        <activity android:name=".Home" />
        <activity android:name=".Login" />
        <activity android:name=".Register" />
        <activity android:name=".GetStarted" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>